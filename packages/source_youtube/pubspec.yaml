name: source_youtube
description: YouTube Music adapter implementing the source_api MusicSource interface.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  source_api:
    path: ../source_api
  youtube_explode_dart: ^3.0.0
  http: ^1.0.0
  meta: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
