name: source_api
description: Pluggable music source abstraction for Spice clients.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  meta: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
