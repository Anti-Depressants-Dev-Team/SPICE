/** Phase 4: client batches local listening history and posts it here. */
export async function POST(_request: Request) {
  return Response.json({ error: 'not_implemented', phase: 4 }, { status: 501 });
}
